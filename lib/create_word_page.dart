import 'package:flutter/material.dart';

class CreateWordPage extends StatelessWidget {
  const CreateWordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Create Word Page'),
    );
  }
}
